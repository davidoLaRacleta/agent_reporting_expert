# Core dependencies (installed automatically via `pip install -e .` too).
python-pptx>=0.6.23
python-docx>=1.1.0
pypdf>=4.0.0
openai>=1.30.0
rich>=13.7.0

# Optional: only needed if PPT_AGENT_LLM_PROVIDER=anthropic.
# anthropic>=0.34.0

# Optional: only needed to run the test suite.
# pytest>=8.0.0
